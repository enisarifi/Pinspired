<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pinspire Landing Page</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/styles/login-style.css">

    <style>

        /* Global Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: #101010;
            color: white;
            overflow-x: hidden;
        }

        header {
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: #64B265;
            padding: 0 2rem;
            position: sticky;
            top: 0;
            z-index: 1000;
            background-image: linear-gradient(to right, #559a56, #79af79, #559a56 );
        }

        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            height: 96px;
            width: 70%;
        }

        .logo {
            display: flex;
            flex-direction: row;
            align-items: center;
            justify-content: space-between;
            gap: 20px;
        }

        .navbar-title {
            font-size:1.7rem;
            font-weight: 900;
            text-shadow: 0 0 5px rgb(255,255,255, .5);
        }



        .navbar-logo {
            width: 70px;
        }
        .nav-links {
            display: flex;
            gap: 1rem;
            list-style: none;
        }

        .nav-links a {
            text-decoration: none;
            color: white;
            font-size: 1.2rem;
            font-weight: 600;
        }

        .auth-buttons button {
            margin-left: 10px;
            padding: 12px 24px;
            border: none;
            cursor: pointer;
            border-radius: 10px;
            font-weight: bold;
            font-size: .9rem;
        }

        .auth-buttons .login-btn {
            background: black;
            color: white;
        }

        .auth-buttons .signup-btn {
            background: white;
            color: black;
        }

        main {
            display: flex;
            min-height: calc(100vh - 80px);
            align-items: center;
            padding: 2rem;
            background: linear-gradient(to top, rgba(0, 0, 0, 1), transparent),
            url("/static/images/icons/aaruto.jpg");
            background-size: cover;
            background-position: center;
        }

        .content-wrapper {
            display: flex;
            justify-content: space-between;
            width: 100%;
            max-width: 1200px;
            margin: auto;
        }

        .promo-section {
            color: white;
            max-width: 40%;
            position: sticky;
            top: 30%;
            display: flex;
            flex-direction: column;
            gap: 60px;
        }

        /* .promo-section p {
            font-size: 3rem;
        } */

        .promo-section p {
            font-size: 1.5rem;
        }

        .signup-form-section {
            background: rgba(255, 255, 255, 0.1);
            background-image: linear-gradient(to bottom, rgba(0, 0, 0, 0.447), rgba(255, 255, 255, 0.1) );
            backdrop-filter: blur(8px);
            padding: 2rem;
            border-radius: 20px;
            max-width: 400px;
            margin: auto;
            height: max-content;
            box-shadow: 0 0 20px black;
        }

        .signup-card h2 {
            font-size: 2rem;
            margin-bottom: 10px;
        }

        .signup-card p {
            color: #cfcfcf;
        }

        form {
            padding-top: 20px;
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        input {
            padding: 0.75rem;
            border-radius: 5px;
            border: none;
        }


        ::-webkit-scrollbar {
            width: 5px;
            height: 5px;
            background-color: #ffffff;
        }
        ::-webkit-scrollbar-thumb {
            background-color: #3a5c3a;
            padding: 1px 5px;
            border-radius: 20px;
        }

        .continue-btn {
            background: #64B265;
            color: white;
            padding: 0.75rem;
            border: none;
            cursor: pointer;
            margin-top: 20px;
            border-radius: 15px;
            transition: 100ms;
            font-size: 1rem;
            font-weight: 600;
        }
        .continue-btn:hover {
            background: #4e8a4f;

        }

        .google-btn {
            background: white;
            color: black;
            padding: 0.75rem;
            border-radius: 5px;
            display: flex;
            justify-content: center;
            align-items: center;
            cursor: pointer;
        }

        @media (max-width: 768px) {
            .content-wrapper {
                flex-direction: column;
                align-items: center;
            }

            .promo-section {
                text-align: center;
                max-width: 100%;
                margin-bottom: 2rem;
            }

            .signup-form-section {
                max-width: 100%;
            }
        }


        .terms-container {
            margin-top: 10px;
            display: flex;
            align-items: center;
        }

        .terms-label {
            font-size: 14px;
            color: rgb(186, 186, 186);
            display: flex;
            align-items: center;
        }

        .terms-label input[type="checkbox"] {
            margin-right: 8px;
            width: 18px;
            height: 18px;
            cursor: pointer;
        }

        .terms-label a.terms-link {
            color: #79AF79; /* Theme color */
            text-decoration: none;
            font-weight: bold;
        }

        .terms-label a.terms-link:hover {
            text-decoration: underline;
        }


        .background-image {
            /* height: 100%; */
            width: 143%;
            position: absolute;
            top: 0;
            z-index: -1;
            transform: translate(-100px);
            /* margin-left: 200px; */
        }

        body {
            overflow: scroll;
            overflow-x: hidden;
            height: max-content;
        }

        /* <section class="promo-section">
            <h1 class="no-account-h">No Account?</h1>
            <h1 class="signup-h">Sign Up To Get Inspiration</h1>
        </section> */

        .promo-section {
            display: flex;
            margin-top: 60px;
            flex-direction: column;
            align-items: center;
            justify-content: space-between;
            height: 300px;
            text-align: center;
        }

        .no-account-h {
            font-size: 1rem;
        }

        .go-to-login-btn {
            /* margin-top: 30px; */
            padding: 12px 24px;
            border: none;
            cursor: pointer;
            border-radius: 10px;
            font-weight: bold;
            font-size: .9rem;
        }


    </style>
</head>
<body>

<img class="background-image" src="/Resources/Screenshot 2024-11-17 200618.png" alt="">

<header>
    <nav class="navbar">
        <div class="logo">
            <img src="/Resources/Ma logo dawg.png" class="navbar-logo" alt="" srcset="">
            <!-- <img src="/images/icons/Ma%20logo%20dawg.png" class="navbar-logo" alt=""> -->
            <p class="navbar-title">Pinspired</p>

        </div>
        <ul class="nav-links">
            <li><a href="#">Explore</a></li>
            <li><a href="#">Watch</a></li>
            <li><a href="#">About</a></li>
            <li><a href="#">Contact</a></li>
            <li><a href="#">Blog</a></li>
        </ul>
        <div class="auth-buttons">
            <button onclick="window.location.href='/llagin.html'" class="login-btn">Log In</button>
            <button  onclick="window.location.href='/register.html'"  class="signup-btn">Sign up</button>
        </div>
    </nav>
</header>

<main>


    <div class="content-wrapper">
        <section class="promo-section">
            <p style="font-size: 2rem; font-weight: 600;" class="no-account-h">Already have an Account?</p>
            <p class="signup-h" style="font-size: 4rem; font-weight: 600;">Sign In To Get Inspiration</p>
            <button onclick="window.location.href='/login'" class="go-to-login-btn">Go to Log In Page</button>

        </section>

        <section class="signup-form-section">
            <div class="signup-card">
                <h2>Welcome To Pinspire</h2>
                <p>Find new ideas to share</p>

                <form th:action="@{/register}" th:object="${registerUserRequestDto}" method="post">

                    <!-- Username Field -->
                    <label for="username">Username</label>
                    <input type="text" id="username" th:field="*{username}" placeholder="Username" required>
                    <p th:if="${#fields.hasErrors('username')}" th:errors="*{username}" style="color:red;"></p>

                    <!-- Email Field -->
                    <label for="email">Email</label>
                    <input type="email" id="email" th:field="*{email}" placeholder="Email" required>
                    <p th:if="${#fields.hasErrors('email')}" th:errors="*{email}" style="color:red;"></p>

                    <!-- Name Field -->
                    <label for="name">Name</label>
                    <input type="text" id="name" th:field="*{name}" placeholder="Name" required>
                    <p th:if="${#fields.hasErrors('name')}" th:errors="*{name}" style="color:red;"></p>
                    
                    <label for="surname">Surname</label>
                    <input type="text" id="surname" th:field="*{surname}" placeholder="Surname" required>
                    <p th:if="${#fields.hasErrors('surname')}" th:errors="*{surname}" style="color:red;"></p>


                    <!-- Birthdate Field -->
                    <label for="birthdate">Birthdate</label>
                    <input type="date" id="birthdate" th:field="*{birthdate}" required>
                    <p th:if="${#fields.hasErrors('birthdate')}" th:errors="*{birthdate}" style="color:red;"></p>

                    <!-- Password Field -->
                    <label for="password">Password</label>
                    <input type="password" id="password" th:field="*{password}" placeholder="Password" required>
                    <p th:if="${#fields.hasErrors('password')}" th:errors="*{password}" style="color:red;"></p>

                    <!-- Confirm Password Field -->
                    <label for="confirmPassword">Confirm Password</label>
                    <input type="password" id="confirmPassword" th:field="*{confirmPassword}" placeholder="Confirm Password" required>
                    <p th:if="${#fields.hasErrors('confirmPassword')}" th:errors="*{confirmPassword}" style="color:red;"></p>

                    <!-- Terms Checkbox -->
                    <div class="terms-container">
                        <label for="acceptTerms">
                            <input type="checkbox" id="acceptTerms" th:field="*{acceptTerms}" required />
                            I accept the <a href="/terms" target="_blank" class="terms-link">terms and conditions</a>.
                        </label>
                    </div>
                    <p th:if="${#fields.hasErrors('acceptTerms')}" th:errors="*{acceptTerms}" style="color:red;"></p>

                    <!-- Submit Button -->
                    <button type="submit" class="continue-btn">Register</button>

                </form>


            </div>
        </section>
    </div>
</main>
</body>
</html>
